# Claude Code Skills для NetworkingCRM

Этот каталог содержит специализированные skills для разработки NetworkingCRM проекта с использованием Claude Code (antigravity).

## Доступные Skills

### 🧪 test-runner
Запускает тесты проекта с правильными настройками.

**Использование:**
```
/test                 # Все тесты
/test unit            # Unit тесты
/test integration     # Integration тесты
/test <path>          # Конкретный тест
```

**Что делает:**
- Запускает pytest с coverage
- Анализирует результаты
- Показывает детали ошибок
- Предлагает исправления

---

### 🗄️ db-migrate
Управляет миграциями базы данных через Alembic.

**Использование:**
```
/migrate create <description>   # Создать миграцию
/migrate up                      # Применить миграции
/migrate down                    # Откатить последнюю
/migrate status                  # Показать статус
/migrate history                 # История миграций
```

**Что делает:**
- Создает autogenerate миграции
- Проверяет корректность
- Применяет с безопасностью
- Управляет версиями схемы

---

### ⚡ add-feature
Добавляет новую фичу следуя архитектуре проекта.

**Использование:**
```
/add-feature <описание>
```

**Пример:**
```
/add-feature экспорт контактов в Notion
```

**Что делает:**
- Создает план реализации
- Следует слоям архитектуры (model → repository → service → handler)
- Пишет тесты сразу
- Проверяет интеграцию

**Создает:**
- Models с валидацией
- Repository методы
- Service логику
- Pydantic schemas
- Handlers (Telegram/API)
- Unit и integration тесты

---

### 🌐 api-endpoint
Создает REST API endpoint с OpenAPI документацией.

**Использование:**
```
/api-endpoint <метод> <путь> <описание>
```

**Примеры:**
```
/api-endpoint GET /api/v1/contacts/:id Get contact by ID
/api-endpoint POST /api/v1/contacts Create new contact
```

**Что делает:**
- Создает Pydantic schemas
- Добавляет route с аннотациями
- Генерирует OpenAPI docs
- Пишет integration тесты
- Проверяет в Swagger UI

---

### 📱 telegram-handler
Создает Telegram bot handler.

**Использование:**
```
/telegram-handler <тип> <описание>
```

**Типы:**
- `command` - для /команд
- `message` - для текстовых сообщений
- `callback` - для inline кнопок
- `voice` - для голосовых
- `contact` - для контактов

**Примеры:**
```
/telegram-handler command /stats - показать статистику
/telegram-handler voice Обработка голосовых для контактов
/telegram-handler callback Действия на карточках контактов
```

**Что делает:**
- Создает handler функцию
- Добавляет middlewares (auth, logging, rate_limit)
- Создает keyboards
- Обрабатывает ошибки
- Пишет integration тесты

---

### 🔧 fix-tests
Автоматически исправляет падающие тесты.

**Использование:**
```
/fix-tests           # Все падающие тесты
/fix-tests <path>    # Конкретный тест
```

**Что делает:**
- Запускает тесты
- Анализирует ошибки
- Исправляет проблемы:
  - Missing async/await
  - Wrong assertions
  - Missing mocks
  - Import errors
  - Fixture issues
- Перезапускает тесты
- Показывает summary

---

### 👀 review-code
Проводит code review изменений.

**Использование:**
```
/review           # Все uncommitted изменения
/review <file>    # Конкретный файл
```

**Проверяет:**
- ✅ Архитектура (layers, separation)
- ✅ Code quality (type hints, docstrings)
- ✅ Async/await корректность
- ✅ Validation (Pydantic, ORM)
- ✅ Error handling
- ✅ Security (SQL injection, XSS)
- ✅ Testing coverage
- ✅ Documentation

**Формат отчета:**
- Severity (critical/major/minor)
- Файл и строка
- Проблема и решение
- Рекомендация (approve/request changes)

---

## Workflow Examples

### Добавление новой фичи
```
1. /add-feature напоминания о follow-up
   → Создает models, services, handlers, tests

2. /migrate create add_reminders_table
   → Создает миграцию

3. /migrate up
   → Применяет миграцию

4. /test
   → Проверяет что все работает

5. /review
   → Code review перед коммитом
```

### Исправление багов
```
1. /test
   → Находит падающие тесты

2. /fix-tests
   → Автоматически исправляет

3. /test
   → Проверяет исправления

4. /review
   → Финальная проверка
```

### Создание API
```
1. /api-endpoint POST /api/v1/reminders Create reminder
   → Создает endpoint с OpenAPI docs

2. /test integration/api/test_reminders.py
   → Проверяет тесты

3. Check http://localhost:8000/api/docs
   → Проверяет Swagger UI
```

### Telegram бот handler
```
1. /telegram-handler command /list - показать контакты
   → Создает handler

2. /test integration/handlers/test_list.py
   → Проверяет тесты

3. Manual test в Telegram
```

---

## Best Practices

### Всегда используй skills для:
- ✅ Добавления новых фич (`/add-feature`)
- ✅ Создания endpoints (`/api-endpoint`)
- ✅ Создания handlers (`/telegram-handler`)
- ✅ Запуска тестов (`/test`)
- ✅ Работы с БД (`/migrate`)

### Workflow для каждой фичи:
1. План → `/add-feature`
2. Реализация → следуй плану
3. Миграция → `/migrate create` если нужно
4. Тесты → `/test`
5. Review → `/review`
6. Commit

### Никогда не:
- ❌ Коммитить без тестов
- ❌ Пропускать `/test` перед коммитом
- ❌ Игнорировать `/review` замечания
- ❌ Применять миграции без проверки
- ❌ Создавать код без плана

---

## Конвенции проекта

См. [claude.md](../../claude.md) для полного гайда по:
- Testing strategy
- OpenAPI specification
- Utilities
- Middlewares
- Error handling
- Pydantic validation
- ORM validation
- Services layer
- Async patterns
- AI integration

---

## Troubleshooting

### Skill не работает
1. Проверь синтаксис команды
2. Убедись что в правильной директории
3. Проверь что файл skill существует

### Тесты падают
```
/test                 # Найди проблему
/fix-tests            # Попробуй автофикс
```

### Миграция не применяется
```
/migrate status       # Проверь текущее состояние
/migrate history      # Посмотри историю
```

### Review показывает много ошибок
1. Прочитай каждую проблему
2. Исправь critical и major
3. Minor можно отложить
4. Перезапусти `/review`

---

## Contributing

При добавлении новых skills:

1. Создай файл `.claude/skills/<name>.md`
2. Следуй формату:
   - Description
   - Usage
   - Instructions
   - Examples
3. Обнови этот README
4. Протестируй skill

**Формат skill файла:**
```markdown
# Skill Name

## Description
Short description

## Usage
- Command syntax
- Examples

## Instructions
Step by step what to do

## Examples
Real examples with input/output
```
